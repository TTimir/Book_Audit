@model IEnumerable<BookAudit.Models.Domain.Publisher>
@{
}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-dark" href="/Publisher/Add">+ Add More Publisher</a>
        </div>
    </div>
    <div class="row">

        <div class="col-md-6">
            <h3> Publishers </h3>

            <table class="table table-stripped table-bordered table-dark border border-4">
                <thead>
                    <tr>
                        <td>Name</td>
                        <td>Action</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.PublisherName</td>
                            <td>
                                <a class="btn btn-success" href="/Publisher/Update?id=@item.Id"><i class="bi bi-pen"></i></a> |
                                <a class="btn btn-danger" onclick="return window.confirm('Are You Sure?')" href="/Publisher/Delete?id=@item.Id"><i class="bi bi-trash"></i></a>
                            </td>
                        </tr>

                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


